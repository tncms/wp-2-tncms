# Upgrading

## API Compatibility

The `/v1` API should remain backward compatible.

Additive fields are allowed in minor releases.

Breaking response changes require a new API namespace such as `/v2`.
